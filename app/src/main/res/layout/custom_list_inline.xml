<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/custom_inline_item_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/custom_toast"
    android:orientation="horizontal">
    <ImageView
        android:id="@+id/customListImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:scaleType="fitXY"
        android:contentDescription="@string/contentDesciption_Adds"
        android:src="@mipmap/foody_logo"/>
    <TextView
        android:id="@+id/customListTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingEnd="5dp"
        android:paddingStart="10dp"
        android:text="xin chào"
        android:textColor="@color/clBlack"
        android:textSize="@dimen/text_size_small"/>
</LinearLayout>